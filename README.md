What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

answer:getElementById can select a single element by id and on the otherhand, getElementsByClassName can select multiple elements by class name. 
querySelector can return single match css query and  querySelectorAll can access multiple css selector.

How do you create and insert a new element into the DOM?

answer:for creating, document.createElement('tag name') and for this we need a parent node and then we will appendChild to the parent node.

What is Event Bubbling and how does it work?
Event Bubbling is happen when elemnt event child to parent node, parent node to document .

What is Event Delegation in JavaScript? Why is it useful?
answer:it can handle child element by usign event listener in parent element.it is useful because it can handle dynamic elements

what is the difference between preventDefault() and stopPropagation() methods?
answer:preventDefault() stops elments default behaviour meanwhile, stopPropagation() stops event bubbling.
